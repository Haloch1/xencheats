services:
  - type: worker
    name: discordbot-mirror
    runtime: node
    plan: starter
    buildCommand: npm ci --omit=dev
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: TOKEN
        sync: false
      - key: NODE_VERSION
        value: 20
